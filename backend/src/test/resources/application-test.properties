# A fresh file-based H2 database per test context: the same engine and locking
# as a real run, without touching the developer's backend/data/ directory.
spring.datasource.url=jdbc:h2:file:./target/test-data/booking-${random.uuid};LOCK_TIMEOUT=10000
